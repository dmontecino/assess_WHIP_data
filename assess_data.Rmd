---
title: "WHIP DATA QUALITY ASSESSMENT"
output: html_document
params:
  spec: spec
  obs: obs
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

```

```{r load packages , echo=FALSE, warning=FALSE, message=FALSE, eval=T}

# library(readxl)


```

```{r load data, echo=FALSE, warning=FALSE, message=FALSE}

# getwd()
# params$datain$datapath
# number_of_columns=619 # counted the number of columns in the original excel
# 
# spec<-read_excel(path = input$datain$datapath, sheet = 1, col_types = rep("text", number_of_columns))
# obs<-read_excel(path = params$datain$datapath, sheet = 2, col_types = rep("text", number_of_columns))

# spec$Country

head(params$spec)
head(params$obs)

```
